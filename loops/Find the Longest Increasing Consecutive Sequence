arr = list(map(int, input().split()))

max_len = 1
curr = 1

for i in range(1, len(arr)):
    if arr[i] > arr[i - 1]:
        curr += 1
    else:
        if curr > max_len:
            max_len = curr
        curr = 1

if curr > max_len:
    max_len = curr

print(max_len)
