arr = list(map(int, input().split()))

visited = []

for i in range(len(arr)):
    if arr[i] in visited:
        continue

    count = 1
    for j in range(i + 1, len(arr)):
        if arr[i] == arr[j]:
            count += 1

    visited.append(arr[i])
    print(arr[i], count)
