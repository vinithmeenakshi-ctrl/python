def duplicates(arr):
    seen = set()
    dup = set()

    for i in arr:
        if i in seen:
            dup.add(i)
        else:
            seen.add(i)

    return list(dup)

arr = list(map(int, input().split()))
print(duplicates(arr))
