def leaders(arr):
    ans = []
    maximum = arr[-1]

    ans.append(maximum)

    for i in range(len(arr)-2, -1, -1):
        if arr[i] > maximum:
            maximum = arr[i]
            ans.append(maximum)

    return ans[::-1]

arr = list(map(int, input().split()))
print(leaders(arr))
