def power(a, b):
    if b == 0:
        return 1
    if b % 2 == 0:
        half = power(a, b // 2)
        return half * half
    return a * power(a, b - 1)

a = int(input())
b = int(input())
print(power(a, b))
