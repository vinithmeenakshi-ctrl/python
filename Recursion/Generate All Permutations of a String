def permute(s, ans=""):
    if len(s) == 0:
        print(ans)
        return

    for i in range(len(s)):
        ch = s[i]
        rem = s[:i] + s[i+1:]
        permute(rem, ans + ch)

permute(input())
